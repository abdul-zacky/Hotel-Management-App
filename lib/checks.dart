import 'package:flutter/material.dart';
import 'package:wisma1/app_view.dart';

class Checks extends StatelessWidget {
  const Checks({super.key});

  @override
  Widget build(BuildContext context) {
    return const AppView();
  }
}